<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Privacy-first contract (ROADMAP + Technical Validation Report,
         privacy checklist item 1-3): ZERO permissions. The app is fully
         offline; on Android 10+ gallery saves need no runtime permission.
         Any future permission addition requires an explicit owner decision
         and a report update. -->
    <application
        android:name=".SengkodeApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Sengkode"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
